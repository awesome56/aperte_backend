Delete a property request owned by the authenticated user.
---
tags:
  - Requests
security:
  - Bearer: []
parameters:
  - in: path
    name: id
    description: The ID of the request to delete
    required: true
    type: integer
responses:
  204:
    description: Request deleted successfully
  401:
    description: Request does not belong to the authenticated user
  404:
    description: Request not found
